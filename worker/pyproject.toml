[project]
name = "tabbuddy-worker"
version = "0.1.0"
description = "TabBuddy async notification worker"
requires-python = ">=3.12"
dependencies = [
    "redis",
    "hiredis",
    "aio-pika",
    "pydantic",
]
